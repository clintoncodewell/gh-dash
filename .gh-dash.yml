# yaml-language-server: $schema=https://gh-dash.dev/schema.json
prSections:
  - title: Mine
    filters: is:open author:@me repo:clintoncodewell/gh-dash updated:>={{ nowModify "-3w" }} sort:updated-desc
    layout:
      author:
        hidden: true
      repoName:
        hidden: true
  - title: Review
    filters: repo:clintoncodewell/gh-dash -author:@me is:open updated:>={{ nowModify "-2.5w" }}
    layout:
      repoName:
        hidden: true
  - title: All
    filters: repo:clintoncodewell/gh-dash
issuesSections:
  - title: Tickets / backlog
    filters: repo:clintoncodewell/gh-dash is:open sort:updated-desc
  - title: Bugs
    filters: repo:clintoncodewell/gh-dash is:open sort:reactions label:bug
  - title: Features
    filters: repo:clintoncodewell/gh-dash is:open sort:reactions label:feat
  - title: High
    filters: repo:clintoncodewell/gh-dash is:open sort:reactions label:high-pri
  - title: Mid
    filters: repo:clintoncodewell/gh-dash is:open sort:reactions label:mid-pri
  - title: Low
    filters: repo:clintoncodewell/gh-dash is:open sort:reactions label:low-pri
  - title: Unlabeled
    filters: repo:clintoncodewell/gh-dash is:open sort:reactions -label:bug -label:feat
  - title: Archive
    filters: repo:clintoncodewell/gh-dash is:closed sort:updated-desc

defaults:
  view: prs
  refetchIntervalMinutes: 0
  layout:
    prs:
      repoName:
        hidden: true
      base:
        hidden: true

  preview:
    open: true
    width: 0.70
    position: right
  prsLimit: 20
  issuesLimit: 20
